@model MovieShopDLL.Entities.Movie

@{
    ViewBag.Title = "MovieDetails";
}

<head>
    <link rel="stylesheet" type="text/css" href="~/Styles/MovieDetailsStylesheet.css">
</head>
<body>
    <div style="width: 50%; height: 70%;">
        <table>
            <thead>Movie Details</thead>
            <tr>
                <td class="type">Title:</td>
                <td class="value">@Model.Title</></td>
                <td rowspan="4">
                    <iframe marginwidth="2" marginheight="2" src="@Model.MovieUrl" frameborder="0" allowfullscreen></iframe>
                </td>
            </tr> 
            <tr>
                <td class="type">Genre:</td>
                <td class="value">@Model.Genre.Name</td>
            </tr>
            <tr>
                <td class="type">Released:</td>
                <td class="value">@Model.Year</td>
            </tr>
            <tr>
                <td class="type">Price:</td>
                <td class="value">@Model.Price</td>
            </tr>
            <tr>
                <td class="type">Summary:</td>
                <td colspan="2">@Model.Description</td>
                <td>
                    <button style="text-decoration:none" class="btn btn-success">@Html.ActionLink("Checkout", "Checkout", new {Model.Id})</button>
                    <a class="btn btn-warning" href="../Index">Back</a>

                </td>
            </tr>  
        </table>
    </div>
</body>
